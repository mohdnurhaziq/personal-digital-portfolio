<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>

        <!-- The suite runs against MySQL, not SQLite, so a dialect difference
             cannot hide a bug until production. It gets its own database:
             RefreshDatabase migrates and truncates whatever it is pointed at,
             and `portfolio` holds the real content.

             Pinned two ways, because `<env>` alone is not enough and the gap
             destroys data:

             - Without force, PHPUnit leaves an already-set variable alone, so
               Compose's DB_DATABASE=portfolio would survive into the run.
             - force fixes getenv() and $_ENV but not $_SERVER, and Laravel's
               env() reads $_SERVER first — so it would still resolve to the
               development database.

             That combination once had RefreshDatabase truncating the live
             development data on every `artisan test` inside the container.

             DB_HOST and DB_PORT are deliberately NOT pinned: the host reaches
             MySQL on 127.0.0.1 at the published port and the container reaches
             it at `db:3306`, so each must keep its own ambient value. Only the
             database *name* is fixed, which is what makes the run safe. -->
        <env name="DB_CONNECTION" value="mysql" force="true"/>
        <env name="DB_DATABASE" value="portfolio_test" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <server name="DB_CONNECTION" value="mysql"/>
        <server name="DB_DATABASE" value="portfolio_test"/>
        <server name="DB_URL" value=""/>

        <!-- Same reasoning, different blast radius: a container or CI runner
             holding real SMTP credentials would otherwise send live mail from
             the contact-form tests. -->
        <env name="MAIL_MAILER" value="array" force="true"/>
        <server name="MAIL_MAILER" value="array"/>

        <!-- Never let the suite depend on whether a dev SSR process happens to
             be running: with it up, tests assert against server-rendered HTML;
             with it down, against an empty shell. -->
        <env name="INERTIA_SSR_ENABLED" value="false" force="true"/>
        <server name="INERTIA_SSR_ENABLED" value="false"/>

        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
