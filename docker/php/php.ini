; Gallery photos are capped at 8 MB by the admin's validation rules, so PHP has
; to accept at least that much or the upload dies before Laravel sees it.
upload_max_filesize = 12M
post_max_size = 16M

memory_limit = 512M
max_execution_time = 120

expose_php = Off
